rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full access during development
    // TODO: Add proper authentication rules before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
